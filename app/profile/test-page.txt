// SIMPLE TEST VERSION - Rename this to page.tsx to test if routing works

'use client';

export default function ProfilePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-purple-50 via-pink-50 to-purple-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-purple-600 mb-4">Profile Page Works! ✅</h1>
        <p className="text-gray-600">If you see this, routing is working correctly.</p>
        <p className="text-gray-600 mt-2">Now you can restore the full page.tsx</p>
      </div>
    </div>
  );
}
